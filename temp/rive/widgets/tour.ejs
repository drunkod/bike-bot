<%
    let menu = locals.entry.all.menu;

    let regions = locals.entry.all.data.regions;

    let { topics = [] } = locals.entry.tour;
-%>
<%#
if item.hide = hide topic
if scenes.hide = hide scenes
%>
<% topics.forEach(item => { -%>
<% if(!item.hide) { -%>
> topic <%- item.name %> includes <% if(!item.includes) { -%>global<%}else{-%><%- item.includes %><%}-%>
    <% item.scenes.forEach((scenes, i) => { -%>
        <% if(!scenes.hide) { -%>

    + <%- scenes.comand.toLowerCase() %>
    - <%- scenes.setter %><% 
    if (scenes.change_text !== ''){-%><%- menu.input %><%}
    -%> <%- scenes.change_text %>\n\n<%- scenes.question %><%- scenes.hint %> <%
     if (scenes.go_to){-%>{topic=<%- scenes.go_to %>}<%}-%><%
      if (scenes.comand !== 'подсказка' && scenes.hint !== ''){-%>{@подсказка}<%}-%>

    <%}}); -%>
    
< topic
<%} -%>
<%}); -%>